apiVersion: v1
kind: Service
metadata:
  name: canary-gateway-nodeport
spec:
  selector:
    app.kubernetes.io/name: canary-gateway-nginx # controller pods
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort
