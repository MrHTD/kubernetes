apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: myapp-route
spec:
  parentRefs:
    - name: myapp-gateway
  hostnames:
    - "*"
  rules:
    - backendRefs:
        - name: myapp
          port: 80
